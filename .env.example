# このファイルを「.env」という名前でコピーして、値を書き換える（.env は Git に入らない）。
# Unityエディタは再生のたびに .env を読み直す。ビルドしたアプリには、ビルドした時点の値が入る。

# 都市APIのベースURL（末尾の / は不要）
# 手元の仮サーバー（python3 "Assets/_Project/Tests~/mock_city_api.py"）を使う場合は http://localhost:8080
CITY_API_BASE_URL=https://api.example.com
